<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
      @!section('title', 'Employee Management System')
    </title>
    @vite(['resources/css/app.css', 'resources/js/app.js'])
  </head>
  <body class="bg-gray-50 min-h-screen">
    @if(user)
      <!-- Navigation -->
      <nav class="bg-white shadow-sm border-b border-gray-200">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between h-16">
            <div class="flex">
              <!-- Logo -->
              <div class="flex-shrink-0 flex items-center">
                <a href="{{ route('employees.index') }}" class="text-xl font-bold text-blue-600">
                  Manajemen Karyawan
              </a>
              </div>
              <!-- Navigation Links -->
              <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
                @if(user.role === 'admin')
                  <!-- Admin Menu -->
                  <a
                    href="{{ route('employees.index') }}"
                    class="border-blue-500 text-gray-900 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium"
                  >
                    Employees
                  </a>
                  <a
                    href="{{ route('absensi.history') }}"
                    class="border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium"
                  >
                    History Absensi
                  </a>
                  <a
                    href="{{ route('employees.create') }}"
                    class="border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium"
                  >
                    Add Employee
                  </a>
                @else
                  <!-- Employee Menu -->
                  <a
                    href="{{ route('absensi.index') }}"
                    class="border-blue-500 text-gray-900 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium"
                  >
                    Absensi
                  </a>
                  <a
                    href="{{ route('employees.profile') }}"
                    class="border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium"
                  >
                    Profile
                  </a>
                @end
              </div>
            </div>
            <!-- User Menu -->
            <div class="flex items-center">
              <span class="text-sm text-gray-700 mr-4">
                {{ user.name }}
                @if(user.role === 'admin')
                  <span class="ml-2 px-2 py-1 text-xs font-semibold text-blue-800 bg-blue-100 rounded-full">Admin</span>
                @end
              </span>
              <form action="{{ route('auth.logout') }}" method="POST" class="inline">
                {{ csrfField() }}
                <button type="submit" class="text-sm text-gray-500 hover:text-gray-700">
                  Logout
              </button>
              </form>
            </div>
          </div>
        </div>
      </nav>
    @end
    
    <!-- Flash Messages -->
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
      @if(flashMessages.has('success'))
        <div
          class="bg-green-50 border border-green-200 text-green-800 px-4 py-3 rounded relative mb-4"
          role="alert"
        >
          <span class="block sm:inline">{{ flashMessages.get('success') }}</span>
        </div>
      @end
      
      @if(flashMessages.has('error'))
        <div
          class="bg-red-50 border border-red-200 text-red-800 px-4 py-3 rounded relative mb-4"
          role="alert"
        >
          <span class="block sm:inline">{{ flashMessages.get('error') }}</span>
        </div>
      @end
      
      @if(flashMessages.has('errors'))
        <div
          class="bg-red-50 border border-red-200 text-red-800 px-4 py-3 rounded relative mb-4"
          role="alert"
        >
          <ul class="list-disc list-inside">
            @each(error in flashMessages.get('errors'))
              <li>
                {{ error.message }}
              </li>
            @end
          </ul>
        </div>
      @end
    </div>

    <!-- Main Content -->
    <main>
      @!section('content')
    </main>

    <!-- Footer -->
    <footer class="bg-white border-t border-gray-200 mt-auto">
      <div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
        <p class="text-center text-sm text-gray-500">
          &copy; {{ new Date().getFullYear() }} Employee Management System. All rights reserved.
        </p>
      </div>
    </footer>
  </body>
</html>
