<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
      Dashboard Admin - Sistem Manajemen Karyawan
    </title>
    @vite(['resources/css/app.css', 'resources/js/app.js'])
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      
      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', sans-serif;
        background: linear-gradient(135deg, #1a1a1a 0%, #2d2d2d 100%);
        min-height: 100vh;
        color: white;
      }
      
      nav {
        background: rgba(255, 255, 255, 0.05);
        backdrop-filter: blur(16px);
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        position: sticky;
        top: 0;
        z-index: 100;
      }
      
      nav .container {
        max-width: 1400px;
        margin: 0 auto;
        padding: 0 20px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        height: 64px;
      }
      
      nav .logo {
        font-size: 20px;
        font-weight: 700;
        color: white;
        text-decoration: none;
      }
      
      nav .nav-links {
        display: flex;
        gap: 32px;
        align-items: center;
      }
      
      nav .nav-links a {
        color: rgba(255, 255, 255, 0.7);
        text-decoration: none;
        font-size: 14px;
        font-weight: 500;
        transition: color 0.2s;
        padding: 8px 0;
        border-bottom: 2px solid transparent;
      }
      
      nav .nav-links a:hover,
      nav .nav-links a.active {
        color: white;
        border-bottom-color: rgba(255, 255, 255, 0.5);
      }
      
      nav .user-section {
        display: flex;
        align-items: center;
        gap: 16px;
      }
      
      nav .badge {
        padding: 4px 10px;
        border-radius: 12px;
        font-size: 11px;
        font-weight: 600;
        background: rgba(255, 255, 255, 0.15);
      }
      
      nav button {
        background: none;
        border: none;
        color: rgba(255, 255, 255, 0.6);
        font-size: 14px;
        cursor: pointer;
        transition: color 0.2s;
      }
      
      nav button:hover {
        color: white;
      }
      
      .main-container {
        max-width: 1400px;
        margin: 0 auto;
        padding: 32px 20px;
      }
      
      .page-header {
        margin-bottom: 32px;
      }
      
      .page-header h1 {
        font-size: 32px;
        font-weight: 700;
        margin-bottom: 8px;
      }
      
      .page-header p {
        font-size: 14px;
        color: rgba(255, 255, 255, 0.6);
      }
      
      .stats-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 20px;
        margin-bottom: 32px;
      }
      
      .stat-card {
        background: rgba(255, 255, 255, 0.05);
        backdrop-filter: blur(16px);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 16px;
        padding: 24px;
        transition: all 0.3s;
      }
      
      .stat-card:hover {
        transform: translateY(-2px);
        border-color: rgba(255, 255, 255, 0.2);
      }
      
      .stat-card .icon {
        width: 48px;
        height: 48px;
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 24px;
        margin-bottom: 16px;
      }
      
      .stat-card.total .icon {
        background: rgba(59, 130, 246, 0.2);
      }
      
      .stat-card.present .icon {
        background: rgba(76, 175, 80, 0.2);
      }
      
      .stat-card.late .icon {
        background: rgba(255, 152, 0, 0.2);
      }
      
      .stat-card.absent .icon {
        background: rgba(244, 67, 54, 0.2);
      }
      
      .stat-card .label {
        font-size: 13px;
        font-weight: 600;
        color: rgba(255, 255, 255, 0.6);
        text-transform: uppercase;
        letter-spacing: 0.5px;
        margin-bottom: 8px;
      }
      
      .stat-card .value {
        font-size: 36px;
        font-weight: 700;
        margin-bottom: 4px;
      }
      
      .stat-card .description {
        font-size: 13px;
        color: rgba(255, 255, 255, 0.5);
      }
      
      .section-title {
        font-size: 20px;
        font-weight: 600;
        margin-bottom: 20px;
      }
      
      .table-container {
        background: rgba(255, 255, 255, 0.05);
        backdrop-filter: blur(16px);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 16px;
        overflow: hidden;
        margin-bottom: 32px;
      }
      
      table {
        width: 100%;
        border-collapse: collapse;
      }
      
      thead {
        background: rgba(255, 255, 255, 0.03);
      }
      
      th {
        padding: 16px 20px;
        text-align: left;
        font-size: 13px;
        font-weight: 600;
        color: rgba(255, 255, 255, 0.6);
        text-transform: uppercase;
        letter-spacing: 0.5px;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
      }
      
      td {
        padding: 16px 20px;
        font-size: 14px;
        border-bottom: 1px solid rgba(255, 255, 255, 0.05);
      }
      
      tbody tr:hover {
        background: rgba(255, 255, 255, 0.02);
      }
      
      tbody tr:last-child td {
        border-bottom: none;
      }
      
      .status-badge {
        display: inline-block;
        padding: 4px 12px;
        border-radius: 12px;
        font-size: 12px;
        font-weight: 600;
      }
      
      .status-present {
        background: rgba(76, 175, 80, 0.2);
        color: #a5d6a7;
        border: 1px solid rgba(76, 175, 80, 0.3);
      }
      
      .status-late {
        background: rgba(255, 152, 0, 0.2);
        color: #ffcc80;
        border: 1px solid rgba(255, 152, 0, 0.3);
      }
      
      .status-absent {
        background: rgba(244, 67, 54, 0.2);
        color: #ef9a9a;
        border: 1px solid rgba(244, 67, 54, 0.3);
      }
      
      .empty-state {
        padding: 48px 20px;
        text-align: center;
        color: rgba(255, 255, 255, 0.5);
      }
      
      .empty-state-icon {
        font-size: 48px;
        margin-bottom: 16px;
        opacity: 0.3;
      }
      
      .btn-link {
        color: #93c5fd;
        text-decoration: none;
        font-size: 14px;
        transition: color 0.2s;
      }
      
      .btn-link:hover {
        color: #bfdbfe;
      }
      
      /* Holidays Section */
      
      .holidays-list {
        padding: 0;
      }
      
      .holiday-item {
        display: flex;
        align-items: center;
        gap: 20px;
        padding: 20px 24px;
        border-bottom: 1px solid rgba(255, 255, 255, 0.05);
        transition: background 0.2s;
      }
      
      .holiday-item:last-child {
        border-bottom: none;
      }
      
      .holiday-item:hover {
        background: rgba(255, 255, 255, 0.02);
      }
      
      .holiday-date {
        flex-shrink: 0;
        width: 60px;
        height: 60px;
        background: rgba(168, 85, 247, 0.15);
        border-radius: 12px;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        border: 1px solid rgba(168, 85, 247, 0.3);
      }
      
      .date-day {
        font-size: 24px;
        font-weight: 700;
        color: #d8b4fe;
        line-height: 1;
      }
      
      .date-month {
        font-size: 12px;
        color: rgba(216, 180, 254, 0.7);
        text-transform: uppercase;
        margin-top: 4px;
      }
      
      .holiday-info {
        flex: 1;
      }
      
      .holiday-info h4 {
        font-size: 16px;
        font-weight: 600;
        color: white;
        margin-bottom: 4px;
      }
      
      .holiday-info p {
        font-size: 13px;
        color: rgba(255, 255, 255, 0.5);
      }
      
      .holiday-badge {
        flex-shrink: 0;
      }
      
      .badge {
        padding: 6px 12px;
        border-radius: 12px;
        font-size: 12px;
        font-weight: 600;
      }
      
      .badge-national {
        background: rgba(239, 68, 68, 0.2);
        color: rgb(248, 113, 113);
        border: 1px solid rgba(239, 68, 68, 0.3);
      }
      
      .badge-religious {
        background: rgba(168, 85, 247, 0.2);
        color: rgb(216, 180, 254);
        border: 1px solid rgba(168, 85, 247, 0.3);
      }
      
      .badge-company {
        background: rgba(34, 197, 94, 0.2);
        color: rgb(134, 239, 172);
        border: 1px solid rgba(34, 197, 94, 0.3);
      }
      
      .badge-other {
        background: rgba(156, 163, 175, 0.2);
        color: rgb(209, 213, 219);
        border: 1px solid rgba(156, 163, 175, 0.3);
      }
    </style>
  </head>
  <body>
    <!-- Navigation -->
    <nav>
      <div class="container">
        <a href="{{ route('dashboard.index') }}" class="logo">Manajemen Karyawan</a>
        <div class="nav-links">
          <a href="{{ route('dashboard.index') }}" class="active">Dashboard</a>
          <a href="{{ route('employees.index') }}">Karyawan</a>
          <a href="{{ route('absensi.history') }}">History Absensi</a>
          <a href="{{ route('holidays.index') }}">Hari Libur</a>
          <a href="{{ route('employees.create') }}">Tambah Karyawan</a>
        </div>
        <div class="user-section">
          <span>{{ user.name }}</span>
          <span class="badge">Admin</span>
          <form action="{{ route('auth.logout') }}" method="POST" style="display: inline;">
            {{ csrfField() }}
            <button type="submit">Logout</button>
          </form>
        </div>
      </div>
    </nav>

    <!-- Main Content -->
    <div class="main-container">
      <!-- Page Header -->
      <div class="page-header">
        <h1>
          Dashboard Admin
        </h1>
        <p>
          Monitoring absensi karyawan - {{ currentDateTime }}
        </p>
      </div>

      <!-- Statistics Cards -->
      <div class="stats-grid">
        <!-- Total Employees -->
        <div class="stat-card total">
          <div class="icon">
            👥
          </div>
          <div class="label">
            Total Karyawan
          </div>
          <div class="value">
            {{ totalEmployees }}
          </div>
          <div class="description">
            Karyawan terdaftar
          </div>
        </div>

        <!-- Today Present -->
        <div class="stat-card present">
          <div class="icon">
            ✅
          </div>
          <div class="label">
            Hadir Hari Ini
          </div>
          <div class="value">
            {{ todayPresent }}
          </div>
          <div class="description">
            Sudah check-in
          </div>
        </div>

        <!-- Today Late -->
        <div class="stat-card late">
          <div class="icon">
            ⏰
          </div>
          <div class="label">
            Terlambat
          </div>
          <div class="value">
            {{ todayLate }}
          </div>
          <div class="description">
            Check-in setelah 09:00
          </div>
        </div>

        <!-- Today Absent -->
        <div class="stat-card absent">
          <div class="icon">
            ❌
          </div>
          <div class="label">
            Belum Absen
          </div>
          <div class="value">
            {{ todayAbsent }}
          </div>
          <div class="description">
            Belum check-in hari ini
          </div>
        </div>
      </div>

      <!-- Today's Attendance -->
      <h2 class="section-title">
        Absensi Hari Ini
      </h2>
      <div class="table-container">
        @if(todayAttendances.length > 0)
          <table>
            <thead>
              <tr>
                <th>
                  Nama Karyawan
                </th>
                <th>
                  Posisi
                </th>
                <th>
                  Departemen
                </th>
                <th>
                  Check-in
                </th>
                <th>
                  Check-out
                </th>
                <th>
                  Status
                </th>
              </tr>
            </thead>
            <tbody>
              @each(attendance in todayAttendances)
                <tr>
                  <td>
                    {{ attendance.employee_id.name }}
                  </td>
                  <td>
                    {{ attendance.employee_id.position }}
                  </td>
                  <td>
                    {{ attendance.employee_id.department }}
                  </td>
                  <td>
                    {{ attendance.checkInFormatted }}
                  </td>
                  <td>
                    {{ attendance.checkOutFormatted }}
                  </td>
                  <td>
                    @if(attendance.status === 'present')
                      <span class="status-badge status-present">Hadir</span>
                    @elseif(attendance.status === 'late')
                      <span class="status-badge status-late">Terlambat</span>
                    @else
                      <span class="status-badge status-absent">Alpha</span>
                    @end
                  </td>
                </tr>
              @end
            </tbody>
          </table>
        @else
          <div class="empty-state">
            <div class="empty-state-icon">
              📋
            </div>
            <p>
              Belum ada absensi hari ini
            </p>
          </div>
        @end
      </div>

      <!-- Recent Attendance -->
      <h2 class="section-title">
        Absensi Terbaru
      </h2>
      <div class="table-container">
        @if(recentAttendances.length > 0)
          <table>
            <thead>
              <tr>
                <th>
                  Tanggal
                </th>
                <th>
                  Nama Karyawan
                </th>
                <th>
                  Posisi
                </th>
                <th>
                  Check-in
                </th>
                <th>
                  Check-out
                </th>
                <th>
                  Status
                </th>
              </tr>
            </thead>
            <tbody>
              @each(attendance in recentAttendances)
                <tr>
                  <td>
                    {{ attendance.dateFormatted }}
                  </td>
                  <td>
                    {{ attendance.employee_id.name }}
                  </td>
                  <td>
                    {{ attendance.employee_id.position }}
                  </td>
                  <td>
                    {{ attendance.checkInFormatted }}
                  </td>
                  <td>
                    {{ attendance.checkOutFormatted }}
                  </td>
                  <td>
                    @if(attendance.status === 'present')
                      <span class="status-badge status-present">Hadir</span>
                    @elseif(attendance.status === 'late')
                      <span class="status-badge status-late">Terlambat</span>
                    @else
                      <span class="status-badge status-absent">Alpha</span>
                    @end
                  </td>
                </tr>
              @end
            </tbody>
          </table>
        @else
          <div class="empty-state">
            <div class="empty-state-icon">
              📊
            </div>
            <p>
              Belum ada data absensi
            </p>
          </div>
        @end
      </div>

      <!-- Quick Actions -->
      <div style="text-align: center; margin-top: 32px;">
        <a href="{{ route('absensi.history') }}" class="btn-link">
          Lihat Semua History Absensi →
        </a>
      </div>

      <!-- Upcoming Holidays Section -->
      <div class="card" style="margin-top: 32px;">
        <div class="card-header">
          <h3 style="font-size: 20px; font-weight: 600;">
            Hari Libur Mendatang
          </h3>
          <a href="{{ route('holidays.index') }}" class="btn-link" style="font-size: 14px;">
            Lihat Semua →
          </a>
        </div>
        @if(upcomingHolidays && upcomingHolidays.length > 0)
          <div class="holidays-list">
            @each(holiday in upcomingHolidays)
              <div class="holiday-item">
                <div class="holiday-date">
                  <div class="date-day">
                    {{ new Date(holiday.date).getDate() }}
                  </div>
                  <div class="date-month">
                    {{ new Date(holiday.date).toLocaleDateString('id-ID', { month: 'short' }) }}
                  </div>
                </div>
                <div class="holiday-info">
                  <h4>
                    {{ holiday.name }}
                  </h4>
                  <p>
                    {{ new Date(holiday.date).toLocaleDateString('id-ID', {                       weekday: 'long',                      year: 'numeric',                       month: 'long',                       day: 'numeric'                     }) }}
                  </p>
                </div>
                <div class="holiday-badge">
                  @if(holiday.type === 'national')
                    <span class="badge badge-national">Nasional</span>
                  @elseif(holiday.type === 'religious')
                    <span class="badge badge-religious">Keagamaan</span>
                  @elseif(holiday.type === 'company')
                    <span class="badge badge-company">Perusahaan</span>
                  @else
                    <span class="badge badge-other">Lainnya</span>
                  @end
                </div>
              </div>
            @end
          </div>
        @else
          <div class="empty-state">
            <div class="empty-state-icon">
              📅
            </div>
            <p style="margin-bottom: 16px;">
              Belum ada hari libur mendatang
            </p>
            <a href="{{ route('holidays.create') }}" class="btn-link">
              + Tambah Hari Libur
            </a>
          </div>
        @end
      </div>
    </div>
  </body>
</html>
